[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "django>=5.2,<5.3",
    "django-environ>=0.12,<1",
    "djangorestframework>=3.16,<4",
    "psycopg[binary]>=3.2,<4",
]

[dependency-groups]
dev = [
    "ruff>=0.12",
]
