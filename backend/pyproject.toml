[project]
name = "ruta-unsa-backend"
version = "0.1.0"
description = "Backend de la plataforma de planificación académica Ruta UNSA"
requires-python = ">=3.14,<3.15"
dependencies = [
    "django>=5.2.8,<5.3",
]
