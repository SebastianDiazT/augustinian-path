info:
  name: Delete Area
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/institution/areas/{{area_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
