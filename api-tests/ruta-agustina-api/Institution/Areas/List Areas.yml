info:
  name: List Areas
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/institution/areas/"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
