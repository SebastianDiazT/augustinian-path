info:
  name: 3. Update Area
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/institution/areas/{{area_id}}/"
  body:
    type: json
    data: |-
      {
        "name": "Ingenierías (Actualizado)"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 200 OK", function() {
          expect(res.getStatus()).to.equal(200);
        });

        test("Name is successfully updated", function() {
          const response = res.getBody();
          expect(response.data.name).to.equal("Ingenierías (Actualizado)");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5