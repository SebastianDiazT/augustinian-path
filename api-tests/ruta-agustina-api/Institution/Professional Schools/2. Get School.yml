info:
  name: 2. Get School
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/institution/professional-schools/{{school_id}}/"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 200 OK", function() {
          expect(res.getStatus()).to.equal(200);
        });

        test("Data matches the created School", function() {
          const response = res.getBody();
          expect(response.data.public_id).to.equal(bru.getEnvVar("school_id"));
          expect(response.data.faculty).to.equal(bru.getEnvVar("faculty_id"));
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5