info:
  name: 2. Get Instructor
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/curricula/instructors/{{instructor_id}}/"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 200 OK", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5