info:
  name: 2. Toggle Favorite
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/schedules/schedule-alternatives/{{alternative_id}}/toggle_favorite/"
  body: none
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 200 OK", function() {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5